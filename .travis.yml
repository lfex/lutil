language: erlang
install: true
before_script:
    - wget https://s3.amazonaws.com/rebar3/rebar3
    - chmod +x rebar3
env: PATH=".:$PATH"
script:
  - make build-all
  - make check
  - make check-gitlab
  #- make check-hexpm # XXX disabled due to Hex.pm failing on R15
otp_release:
  - 19.1
  - 18.3
  - 17.5
  - R16B03-1
  - R15B03
